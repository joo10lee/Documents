import { PhoneFrame } from "@/components/feelflow/phone-frame"
import { StrategySelection } from "@/components/feelflow/strategy-selection"

export default function StrategyPage() {
  return (
    <PhoneFrame>
      <StrategySelection />
    </PhoneFrame>
  )
}
