import { PhoneFrame } from "@/components/feelflow/phone-frame"
import { CheckinComplete } from "@/components/feelflow/checkin-complete"

export default function CompletePage() {
  return (
    <PhoneFrame>
      <CheckinComplete />
    </PhoneFrame>
  )
}
